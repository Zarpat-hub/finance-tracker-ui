import { styled } from '@mui/system'
import { Link } from 'react-router-dom'

export const StyledLink = styled(Link)({
  textDecoration: 'none',
  color: '#1976d2',
  fontWeight: 'bold',
  paddingLeft: '0.25rem',
})
